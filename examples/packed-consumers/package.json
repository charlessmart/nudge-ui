{
  "name": "packed-consumers",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "vitest run scripts/*.test.mjs",
    "test:e2e": "node scripts/run-packed-consumers.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
