import React from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("app")).render(
  <main>
    <h1>Packed Vite React consumer</h1>
    <button type="button">Inspect me</button>
  </main>,
);
