/// <reference types="vite/client" />

declare module "virtual:nudge-ui-inspector" {
  // Side-effect-only module: the plugin emits the demo inspector bootstrap
  // for it. It has no runtime exports.
  export {};
}
