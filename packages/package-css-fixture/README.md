# `@nudge-ui/package-css-fixture`

This is a private CSS fixture package used by Nudge UI consumer tests. It
contains test theme styles and the fixture contract used to verify package
stylesheet discovery.

It is not published to npm and is not intended for application dependencies.
